# PR #80 전환 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 3D 팝업북을 걷어내고 홈을 3차 "책상 위 히어로"로 되돌린 뒤, PR #80(카페 홈 + 주식앱 `/event` 이전)을 머지·배포해 카페 홈을 전환한다.

**Architecture:** 팝업북 착수 직전 커밋 `8d88a8e`의 홈 코드 3개 파일을 그대로 복원하고, 팝업북 전용 파일·에셋·의존성을 삭제한다. 이후 사장님 컨펌 → 머지 → prod 마이그레이션·배치 확인.

**Tech Stack:** Next.js 16.2, npm, git, Supabase CLI, Vercel.

**스펙:** `docs/superpowers/specs/2026-09-23-book-home-design.md` §2

## Global Constraints

- 워크트리 `/Users/jefflee/workspace/naraka-home-wt` (브랜치 `feat/naraka-home`). `main` 직접 수정 금지.
- 커밋 메시지·주석·문서 한국어. 커밋 끝에 `Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>`.
- 머지·prod 반영은 **사장님(사용자) 명시 승인 후에만**. prod 자격증명이 필요한 명령은 사용자가 `!` 접두어로 직접 실행.
- dev 서버 등 백그라운드 프로세스는 워치독 패턴으로: `( cmd & pid=$!; ( sleep N; kill -9 $pid 2>/dev/null ) & wd=$!; wait $pid; kill $wd 2>/dev/null )`, 끝나면 잔존 프로세스 정리.
- 검증 뷰포트: 390×844, 1440×900.

---

### Task 1: 팝업북 제거 + 홈을 `8d88a8e` 상태로 복원

**Files:**
- Restore (from `8d88a8e`): `src/app/(home)/page.tsx`, `src/app/(home)/home.css`, `src/components/home/DeskHero.tsx`
- Delete: `src/components/home/book/` (전체), `src/lib/homeBook.ts`, `src/lib/homeBook.test.ts`, `src/lib/bookCamera.ts`, `src/lib/bookCamera.test.ts`, `src/lib/bookScroll.ts`, `src/lib/bookScroll.test.ts`, `scripts/book/` (전체, `__pycache__` 포함), `public/home/book/` (전체)
- Modify: `package.json`, `package-lock.json` (three·R3F·drei·@types/three 제거)

**Interfaces:**
- Consumes: 없음
- Produces: 팝업북 없는 `feat/naraka-home` — Plan B(`2026-09-23-book-home.md`)의 출발점

- [ ] **Step 1: 복원 대상이 팝업북 커밋에서만 바뀌었는지 확인**

Run: `git log --oneline 8d88a8e..HEAD -- "src/app/(home)/page.tsx" "src/app/(home)/home.css" src/components/home/DeskHero.tsx`
Expected: `518ae70`, `15d4d93`, `05e9b9d` 등 `feat(book)`/`fix(book)` 커밋만 나온다. 다른 커밋이 섞여 있으면 멈추고 보고한다.

- [ ] **Step 2: 홈 파일 3개 복원**

```bash
git checkout 8d88a8e -- "src/app/(home)/page.tsx" "src/app/(home)/home.css" src/components/home/DeskHero.tsx
```

- [ ] **Step 3: 팝업북 파일·에셋 삭제**

```bash
git rm -r -q src/components/home/book src/lib/homeBook.ts src/lib/homeBook.test.ts \
  src/lib/bookCamera.ts src/lib/bookCamera.test.ts src/lib/bookScroll.ts src/lib/bookScroll.test.ts \
  scripts/book public/home/book
rm -rf scripts/book
```

- [ ] **Step 4: 의존성 제거**

```bash
npm uninstall three @react-three/fiber @react-three/drei @types/three
```

Expected: `package.json`에서 4개가 사라진다. `rm package-lock.json` 후 재생성은 금지(과거 1405줄 손상 사례) — `npm uninstall`이 lock을 갱신하게 둔다.

- [ ] **Step 5: 잔존 참조 0건 확인**

Run: `grep -rnE "three|@react-three|homeBook|bookCamera|bookScroll|PopupBook|components/home/book" src --include=*.ts --include=*.tsx`
Expected: 출력 없음.

- [ ] **Step 6: 게이트**

Run: `npx vitest run && npx eslint src && npx tsc --noEmit && npm run build`
Expected: 모두 통과. (vitest는 `homeCalendar.test.ts` 등 남은 테스트만 실행)

- [ ] **Step 7: 실앱 확인**

```bash
( npm run dev > /tmp/claude-dev.log 2>&1 & pid=$!; ( sleep 240; kill -9 $pid 2>/dev/null ) & wd=$!; wait $pid; kill $wd 2>/dev/null ) &
```

dev 서버가 뜨면 Playwright MCP로 `http://localhost:3000/`을 390×844·1440×900에서 스크린샷. 확인: 책상 위 히어로(좌 이름·CTA, 우 인스타 프레임 영상 루프), 달력, 새 소식, 오시는 길이 보이고 콘솔 에러 0. 확인 후 dev 서버 종료(`pkill -f "next dev"`).

- [ ] **Step 8: 커밋**

```bash
git add -A src package.json package-lock.json scripts public
git commit -m "refactor: 3D 팝업북 폐기 — 홈을 책상 위 히어로(8d88a8e)로 복원, three 계열 의존성 제거

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: 사장님 컨펌 → PR #80 머지 → 배포 (사용자 게이트)

**Files:** 없음 (운영 작업)

**Interfaces:**
- Consumes: Task 1 커밋
- Produces: `main`에 카페 홈 + `/event` 주식앱. Plan B는 이 `main`에서 분기

- [ ] **Step 1: push + PR 본문 갱신**

```bash
git push origin feat/naraka-home
gh pr view 80 --json body -q .body > /tmp/claude-pr80-body.md
```

본문 맨 위에 아래 단락을 추가해 `gh pr edit 80 --body-file /tmp/claude-pr80-body.md`:

```
> 2026-09-23: 3D 팝업북은 폐기하고 홈을 "책상 위 히어로"(3차)로 되돌린 상태로 먼저 전환합니다.
> 책 8권 홈은 별도 브랜치 `feat/book-home`에서 진행합니다 (스펙 docs/superpowers/specs/2026-09-23-book-home-design.md).
```

- [ ] **Step 2: 사장님 컨펌 요청**

Task 1 Step 7 스크린샷 4장(390·1440 × 홈 첫 화면·달력)을 사용자에게 보여주고 머지 승인을 받는다. **승인 전엔 다음 단계로 가지 않는다.**

- [ ] **Step 3: 머지**

Run: `gh pr merge 80 --merge`
Expected: merged. Vercel이 `main`을 자동 배포.

- [ ] **Step 4: prod 마이그레이션 (사용자가 직접 실행)**

사용자에게 안내: `! npx supabase db push` — 마이그 `20260824000000`(홈 콘텐츠 테이블·Storage 버킷)이 적용 목록에 있는지 확인. 이미 적용돼 있으면 "Remote database is up to date".

- [ ] **Step 5: 배포 확인**

- `https://<도메인>/` → 카페 홈(책상 위 히어로)
- `https://<도메인>/event` → 주식앱 로그인/홈
- `https://<도메인>/api/cron/daily-batch`는 `/api/*` 경로 유지라 pg_cron 무영향 — `select status, return_message from cron.job_run_details order by start_time desc limit 1;`로 최근 배치 성공 확인(사용자 실행)

- [ ] **Step 6: 메모리 갱신**

`naraka-home-homepage-project.md`의 "남은 것 ①"을 완료로 표시하고 머지 날짜를 적는다.
